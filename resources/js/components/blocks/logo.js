import React, { Component } from 'react';
import ReactDOM from 'react-dom';

export default class Logo extends Component {
	render() {
        return (
			<div className="header-logo">Kodiak Charts</div>
		);
	}
}